import { counterReducer } from './model/slice/counterSlise';
import { Counter } from './ui/Counter';
import { CounterSchema } from './model/types/counterSchema';

export {
    counterReducer,
    Counter,
    CounterSchema,
};
